# 测试报告

- 是否通过：True
- 用例总数：10
- 通过数量：10
- 失败数量：0

## 测试用例结果

### index.html 文件存在

- 状态：通过
- 预期：生成目录中存在 index.html
- 实际：文件存在
- 说明：检查源码文件是否已落盘。

### style.css 文件存在

- 状态：通过
- 预期：生成目录中存在 style.css
- 实际：文件存在
- 说明：检查源码文件是否已落盘。

### script.js 文件存在

- 状态：通过
- 预期：生成目录中存在 script.js
- 实际：文件存在
- 说明：检查源码文件是否已落盘。

### HTML 正确引用 CSS 和 JS

- 状态：通过
- 预期：index.html 包含 href="style.css" 和 src="script.js"
- 实际：引用完整
- 说明：静态页面必须把样式和脚本接入入口 HTML。

### 页面包含游戏区域

- 状态：通过
- 预期：HTML 中存在 canvas，或存在可识别的 game/board/grid 游戏区域
- 实际：找到游戏区域
- 说明：HTML 长度：764 字符。

### 页面包含分数显示

- 状态：通过
- 预期：HTML 中存在分数文本，或 id/class 包含 score 的元素
- 实际：找到分数显示
- 说明：分数显示用于验证吃到食物后的计分反馈。

### 页面包含重新开始按钮

- 状态：通过
- 预期：HTML 中存在重新开始按钮
- 实际：找到重新开始按钮
- 说明：重新开始按钮用于游戏结束后恢复初始状态。

### JS 包含方向键监听

- 状态：通过
- 预期：script.js 中监听 keydown，并处理方向键
- 实际：找到方向键监听
- 说明：keydown=True，方向键命中特征数=4。

### JS 包含碰撞检测

- 状态：通过
- 预期：script.js 中存在撞墙或撞到自身后的游戏结束逻辑
- 实际：找到碰撞检测逻辑
- 说明：碰撞命名=True，边界检测=True，自身检测=True。

### JS 包含计分逻辑

- 状态：通过
- 预期：script.js 中存在分数状态、加分逻辑和页面更新逻辑
- 实际：找到计分逻辑
- 说明：分数状态=True，加分=True，页面更新=True。

## 缺陷列表

无

## 总结

阶段 5A 静态测试通过。测试目录：generated/snake-game；通过 10 项，失败 0 项。
